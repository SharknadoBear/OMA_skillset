interface:
  display_name: "FVCOM Grid Generation"
  short_description: "Generate benchmark-ready Gmsh-6 FVCOM grids"
  default_prompt: "Use $fvcom-grid-generation to revalidate the active v2 open-exterior boundary contract, fetch bathymetry from the assembled wet domain plus halo, require complete delivered-OBC bathymetry support, generate a deterministic Gmsh-6 FVCOM grid, close valence and superthin debt, classify regional refinement debt, and publish a fully audited project with the standard bathymetric review map."
