interface:
  display_name: "FVCOM Grid Generation"
  short_description: "Orchestrate benchmark-ready FVCOM grid generation"
  default_prompt: "Use $fvcom-grid-generation to invoke $fvcom-region-bpoly, $fvcom-bdry-arc, and $cudem-bathy as returning subworkflows, then generate and audit a deterministic Gmsh-6 FVCOM grid."
