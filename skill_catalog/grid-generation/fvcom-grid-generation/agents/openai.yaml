interface:
  display_name: "FVCOM Grid Generation"
  short_description: "Generate and condition adaptive FVCOM meshes"
  default_prompt: "Use $fvcom-grid-generation to generate or condition an FVCOM 2DM mesh with protected boundaries, superthin connectivity closure, and FVCOM quality gates."
