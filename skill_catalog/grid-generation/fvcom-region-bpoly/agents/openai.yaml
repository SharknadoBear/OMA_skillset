interface:
  display_name: "FVCOM RegionBPoly"
  short_description: "Build and visually gate FVCOM mission envelopes"
  default_prompt: "Use $fvcom-region-bpoly to build a feature-first RegionBPoly, inspect every coastal non-offshore side for waterway truncation, apply only authorized one-side expansion for at most three attempts, and deliver hash-bound visual evidence."
