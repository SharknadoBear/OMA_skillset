interface:
  display_name: "FVCOM Boundary Arc"
  short_description: "Invoke RegionBPoly and build FVCOM boundary arcs"
  default_prompt: "Use $fvcom-bdry-arc to invoke $fvcom-region-bpoly, accept its returned geometry regardless of upstream review or delivery labels, then generate the boundary once, preserve Boundary Arc's physical landfall and topology gates, classify residual exterior roles, and emit the active v2 contract without changing the returned RegionBPoly."
