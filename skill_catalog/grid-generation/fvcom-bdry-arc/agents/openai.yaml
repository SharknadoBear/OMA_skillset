interface:
  display_name: "FVCOM Boundary Arc"
  short_description: "Prepare FVCOM open arcs and model boundary loops"
  default_prompt: "Use $fvcom-bdry-arc to find coastline/bpoly anchors, deform the seaward bpoly chain into a smooth open boundary, combine it with GSHHS land topology, and automatically produce the continuous model outer boundary loop with separate island polygons."
