interface:
  display_name: "FVCOM Boundary Arc"
  short_description: "Build mandatory Adaptive v2 FVCOM boundary arcs"
  default_prompt: "Use $fvcom-bdry-arc to invoke $fvcom-region-bpoly for a direct request or consume its same-run parent return, then build a scientifically defensible Adaptive v2 boundary package without changing the RegionBPoly."
