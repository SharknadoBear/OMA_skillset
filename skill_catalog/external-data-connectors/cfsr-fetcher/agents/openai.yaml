interface:
  display_name: "CFSR Fetcher"
  short_description: "Fetch NCEI-first CFSR atmospheric fields"
  default_prompt: "Use $cfsr-fetcher to route, inventory, estimate, fetch, and validate bounded NCEI-first CFSR atmospheric fields."
