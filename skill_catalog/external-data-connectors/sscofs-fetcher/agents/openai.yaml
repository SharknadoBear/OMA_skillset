interface:
  display_name: "SSCOFS Fetcher"
  short_description: "Plan, fetch, and QA NOAA SSCOFS fields."
  default_prompt: "Use $sscofs-fetcher to plan, download, extract, and health-check bounded NOAA SSCOFS data from operational AWS with NCEI long-term fallback."
