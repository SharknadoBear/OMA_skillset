interface:
  display_name: "Cloud VM Bridge"
  short_description: "SSH bridge for automodeldev01 VM."
  default_prompt: "Use $cloudvm-bridge to connect to the cloud VM, run commands, transfer files, and collect results through the local bridge."
