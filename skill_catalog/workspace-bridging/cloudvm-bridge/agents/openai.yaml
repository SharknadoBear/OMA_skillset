interface:
  display_name: "Cloud VM Bridge"
  short_description: "Private runtime SSH bridge for a cloud VM"
  default_prompt: "Use $cloudvm-bridge with a privately supplied SSH target to run commands or transfer approved files."
